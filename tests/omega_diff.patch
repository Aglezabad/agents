# Test Input: OMEGA — PR Diff for Review

Review the following diff:

```diff
diff --git a/src/utils.py b/src/utils.py
index 3a4b5c6..7d8e9f0 100644
--- a/src/utils.py
+++ b/src/utils.py
@@ -10,7 +10,7 @@ def process_items(items):
     results = []
     for i in range(len(items)):
         item = items[i]
-        if item.value > 100:
+        if item.value >= 100:
             results.append(item)
     return results
 
@@ -25,6 +25,12 @@ def calculate_total(orders):
     total = 0
     for order in orders:
         total += order.price * order.qty
+        # Apply discount for bulk orders
+        if order.qty > 10:
+            total -= order.price * 0.05  # Bug: should be order.price * order.qty * 0.05
     return total
 
+def helper_func(x, y):
+    # TODO: implement this later
+    pass
+
 def format_currency(amount):
-    return "$" + str(amount)
+    return "$" + str(round(amount, 2))
```

Issues intentionally present:
1. **Bug (Major):** The bulk discount calculation is wrong: `order.price * 0.05` instead of `order.price * order.qty * 0.05`. This undercharges customers.
2. **Style (Minor):** Empty `helper_func` with TODO comment should not be committed.
3. **Logic change:** `>` changed to `>=` — might be intentional but should be verified.
